- project:
    name: promise
    project: '{name}'
    jobs:
        - '{project}-verify'
        - '{project}-merge-{stream}'

    # stream:    branch with - in place of / (eg. stable-arno)
    # branch:    branch (eg. stable/arno)
    stream:
        - master:
            branch: 'master'
