---
- project:
    name: cirv-tox-verify

    project: 'cirv'
    project-name: 'cirv'
    build-days-to-keep: 7

    stream:
      - master

    disabled: false

    jobs:
      - '{project-name}-python-jobs'
