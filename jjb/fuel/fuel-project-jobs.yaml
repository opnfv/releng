---
########################
# Job configuration for fuel
########################
- project:
    name: fuel-project-jobs

    project: 'fuel'

    installer: 'fuel'

    stream:
      - master:
          branch: '{stream}'
          gs-pathname: ''
          disabled: false
      - gambia:
          branch: 'stable/{stream}'
          gs-pathname: '/{stream}'
          disabled: false
