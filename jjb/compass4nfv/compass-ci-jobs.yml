- project:

    name: compass-ci

    installer: 'compass'

    project: 'compass4nfv'

    master: &master
        stream: master
        branch: '{stream}'
        gs-pathname: ''
    brahmaputra: &brahmaputra
        stream: brahmaputra
        branch: 'stable/{stream}'
        gs-pathname: '/{stream}'

#--------------------------------
# POD, INSTALLER, AND BRANCH MAPPING
#--------------------------------
    pod:
        - baremetal
        - virtual
        - intel-pod8

    scenario:
        - 'os-nosdn-nofeature-ha-trusty':
            disabled: false
            auto-trigger-name: 'compass-{scenario}-{pod}-trigger'
            os-version: 'trusty'
            <<: *brahmaputra
        - 'os-odl_l2-nofeature-ha-trusty':
            disabled: false
            auto-trigger-name: 'compass-{scenario}-{pod}-trigger'
            os-version: 'trusty'
            <<: *brahmaputra
        - 'os-onos-nofeature-ha-trusty':
            disabled: false
            auto-trigger-name: 'compass-{scenario}-{pod}-trigger'
            os-version: 'trusty'
            <<: *brahmaputra
        - 'os-nosdn-nofeature-ha-trusty':
            disabled: false
            auto-trigger-name: 'compass-{scenario}-{pod}-trigger'
            os-version: 'trusty'
            <<: *master
        - 'os-odl_l2-nofeature-ha-trusty':
            disabled: false
            auto-trigger-name: 'compass-{scenario}-{pod}-trigger'
            os-version: 'trusty'
            <<: *master
        - 'os-onos-nofeature-ha-trusty':
            disabled: false
            auto-trigger-name: 'compass-{scenario}-{pod}-trigger'
            os-version: 'trusty'
            <<: *master
        - 'os-odl_l3-nofeature-ha-trusty':
            disabled: false
            auto-trigger-name: 'compass-{scenario}-{pod}-trigger'
            os-version: 'trusty'
            <<: *master
        - 'os-ocl-nofeature-ha-trusty':
            disabled: false
            auto-trigger-name: 'compass-{scenario}-{pod}-trigger'
            os-version: 'trusty'
            <<: *master
        - 'os-nosdn-nofeature-ha-centos7':
            disabled: false
            auto-trigger-name: 'compass-{scenario}-{pod}-trigger'
            os-version: 'centos7'
            <<: *master
        - 'os-odl_l2-nofeature-ha-centos7':
            disabled: false
            auto-trigger-name: 'compass-{scenario}-{pod}-trigger'
            os-version: 'centos7'
            <<: *master
        - 'os-odl_l3-nofeature-ha-centos7':
            disabled: false
            auto-trigger-name: 'compass-{scenario}-{pod}-trigger'
            os-version: 'centos7'
            <<: *master
        - 'os-onos-nofeature-ha-centos7':
            disabled: false
            auto-trigger-name: 'compass-{scenario}-{pod}-trigger'
            os-version: 'centos7'
            <<: *master
        - 'os-ocl-nofeature-ha-centos7':
            disabled: false
            auto-trigger-name: 'compass-{scenario}-{pod}-trigger'
            os-version: 'centos7'
            <<: *master

    jobs:
        - 'compass-{scenario}-{pod}-daily-{stream}'
        - 'compass-deploy-{pod}-daily-{stream}'

########################
# job templates
########################
- job-template:
    name: 'compass-{scenario}-{pod}-daily-{stream}'

    concurrent: true

    properties:
        - throttle:
            enabled: true
            max-per-node: 1
        - build-blocker:
            use-build-blocker: true
            blocking-jobs:
                - 'compass-os-.*?-{pod}-daily-.*?'
                - 'compass-verify-[^-]*'
            block-level: 'NODE'

    wrappers:
        - build-name:
            name: '$BUILD_NUMBER - Scenario: $DEPLOY_SCENARIO'

    parameters:
        - project-parameter:
            project: '{project}'
        - compass-ci-parameter:
            installer: '{installer}'
            gs-pathname: '{gs-pathname}'
        - string:
            name: DEPLOY_SCENARIO
            default: '{scenario}'
        - 'huawei-{pod}-defaults'
        - '{installer}-defaults'

    triggers:
        - '{auto-trigger-name}'

    builders:
        - trigger-builds:
            - project: 'compass-deploy-{pod}-daily-{stream}'
              current-parameters: false
              predefined-parameters: |
                DEPLOY_SCENARIO={scenario}
                COMPASS_OS_VERSION={os-version}
              same-node: true
              block: true
        - trigger-builds:
            - project: 'functest-compass-{pod}-daily-{stream}'
              current-parameters: false
              predefined-parameters:
                DEPLOY_SCENARIO={scenario}
              same-node: true
              block: true
              block-thresholds:
                build-step-failure-threshold: 'never'
                failure-threshold: 'never'
                unstable-threshold: 'FAILURE'
        - trigger-builds:
            - project: 'yardstick-compass-{pod}-daily-{stream}'
              current-parameters: false
              predefined-parameters:
                DEPLOY_SCENARIO={scenario}
              block: true
              same-node: true
              block-thresholds:
                build-step-failure-threshold: 'never'
                failure-threshold: 'never'
                unstable-threshold: 'FAILURE'

- job-template:
    name: 'compass-deploy-{pod}-daily-{stream}'

    disabled: false

    concurrent: true

    properties:
        - throttle:
            enabled: true
            max-per-node: 1
        - build-blocker:
            use-build-blocker: true
            blocking-jobs:
                - 'compass-deploy-{pod}-daily-.*?'
                - 'compass-verify-deploy-.*?'
            block-level: 'NODE'

    wrappers:
        - build-name:
            name: '$BUILD_NUMBER - Scenario: $DEPLOY_SCENARIO'
        - timeout:
            timeout: 120
            abort: true

    parameters:
        - project-parameter:
            project: '{project}'
        - compass-ci-parameter:
            installer: '{installer}'
            gs-pathname: '{gs-pathname}'
        - 'huawei-{pod}-defaults'
        - '{installer}-defaults'

    scm:
        - git-scm:
            credentials-id: '{ssh-credentials}'
            refspec: ''
            branch: '{branch}'

    wrappers:
        - build-name:
            name: '$BUILD_NUMBER - Scenario: $DEPLOY_SCENARIO'


    builders:
        - shell:
            !include-raw-escape: ./compass-download-artifact.sh
        - shell:
            !include-raw-escape: ./compass-deploy.sh

    publishers:
        - archive:
            artifacts: 'ansible.log'
            allow-empty: 'true'
            fingerprint: true

########################
# parameter macros
########################
- parameter:
    name: compass-ci-parameter
    parameters:
        - string:
            name: BUILD_DIRECTORY
            default: $WORKSPACE/build_output
            description: "Directory where the build artifact will be located upon the completion of the build."
        - string:
            name: GS_URL
            default: '$GS_BASE{gs-pathname}'
            description: "URL to Google Storage."
        - choice:
            name: COMPASS_OPENSTACK_VERSION
            choices:
                - 'liberty'
                - 'juno'

########################
# trigger macros
########################
- trigger:
    name: 'compass-os-nosdn-nofeature-ha-trusty-baremetal-trigger'
    triggers:
        - timed: '0 3 * * *'
- trigger:
    name: 'compass-os-odl_l2-nofeature-ha-trusty-baremetal-trigger'
    triggers:
        - timed: '0 21 * * *'
- trigger:
    name: 'compass-os-odl_l3-nofeature-ha-trusty-baremetal-trigger'
    triggers:
        - timed: ''
- trigger:
    name: 'compass-os-onos-nofeature-ha-trusty-baremetal-trigger'
    triggers:
        - timed: '0 15 * * *'
- trigger:
    name: 'compass-os-ocl-nofeature-ha-trusty-baremetal-trigger'
    triggers:
        - timed: '0 9 * * *'
- trigger:
    name: 'compass-os-nosdn-nofeature-ha-centos7-baremetal-trigger'
    triggers:
        - timed: '0 3 * * * '
- trigger:
    name: 'compass-os-odl_l2-nofeature-ha-centos7-baremetal-trigger'
    triggers:
        - timed: '0 21 * * *'
- trigger:
    name: 'compass-os-odl_l3-nofeature-ha-centos7-baremetal-trigger'
    triggers:
        - timed: ''
- trigger:
    name: 'compass-os-onos-nofeature-ha-centos7-baremetal-trigger'
    triggers:
        - timed: '0 15 * * *'
- trigger:
    name: 'compass-os-ocl-nofeature-ha-centos7-baremetal-trigger'
    triggers:
        - timed: '0 9 * * *'
- trigger:
    name: 'compass-os-nosdn-nofeature-ha-intel-pod8-trigger'
    triggers:
        - timed: ''
- trigger:
    name: 'compass-os-odl_l2-nofeature-ha-intel-pod8-trigger'
    triggers:
        - timed: ''
- trigger:
    name: 'compass-os-odl_l3-nofeature-ha-intel-pod8-trigger'
    triggers:
        - timed: ''
- trigger:
    name: 'compass-os-onos-nofeature-ha-intel-pod8-trigger'
    triggers:
        - timed: ''
- trigger:
    name: 'compass-os-ocl-nofeature-ha-intel-pod8-trigger'
    triggers:
        - timed: ''
- trigger:
    name: 'compass-os-nosdn-nofeature-ha-trusty-virtual-trigger'
    triggers:
        - timed: '0 3 * * *'
- trigger:
    name: 'compass-os-odl_l2-nofeature-ha-trusty-virtual-trigger'
    triggers:
        - timed: '0 21 * * *'
- trigger:
    name: 'compass-os-odl_l3-nofeature-ha-trusty-virtual-trigger'
    triggers:
        - timed: ''
- trigger:
    name: 'compass-os-onos-nofeature-ha-trusty-virtual-trigger'
    triggers:
        - timed: '0 15 * * *'
- trigger:
    name: 'compass-os-ocl-nofeature-ha-trusty-virtual-trigger'
    triggers:
        - timed: '0 9 * * *'
- trigger:
    name: 'compass-os-nosdn-nofeature-ha-centos-virtual-trigger'
    triggers:
        - timed: ''
- trigger:
    name: 'compass-os-odl_l2-nofeature-ha-centos-virtual-trigger'
    triggers:
        - timed: ''
- trigger:
    name: 'compass-os-odl_l3-nofeature-ha-centos-virtual-trigger'
    triggers:
        - timed: ''
- trigger:
    name: 'compass-os-onos-nofeature-ha-centos-virtual-trigger'
    triggers:
        - timed: ''
- trigger:
    name: 'compass-os-ocl-nofeature-ha-centos-virtual-trigger'
    triggers:
        - timed: ''
