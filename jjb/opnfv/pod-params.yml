- parameter:
    name: 'opnfv-jump-1'
    parameters:
        - node:
            name: SLAVE_NAME
            description: 'Slave name on Jenkins'
            allowed-slaves:
                - opnfv-jump-1
            default-slaves:
                - opnfv-jump-1
        - string:
            name: INSTALLER_VERSION
            default: latest
            description: 'Version of the installer to deploy'
        - string:
            name: GIT_BASE
            default: https://gerrit.opnfv.org/gerrit/$PROJECT
            description: 'Git URL to use on the jumphost'
        - string:
            name: SSH_KEY
            default: /root/.ssh/id_rsa
            description: 'SSH key to use for Apex'

- parameter:
    name: 'opnfv-jump-2'
    parameters:
        - node:
            name: SLAVE_NAME
            description: 'Slave name on Jenkins'
            allowed-slaves:
                - opnfv-jump-2
            default-slaves:
                - opnfv-jump-2
        - string:
            name: INSTALLER_VERSION
            default: stable
            description: 'Version of the installer to deploy'
        - string:
            name: GIT_BASE
            default: ssh://gerrit.opnfv.org:29418/$PROJECT
            description: 'Git URL to use on the jumphost'
        - string:
            name: YARDSTICK_TEST_SUITE
            default: opnfv_${{NODE_NAME}}_{loop}.yaml
            description: 'Path to test suite'
        - string:
            name: YARDSTICK_DB_BACKEND
            default: 'db_args'
            description: 'Arguments to use in order to choose the backend DB'

- parameter:
    name: 'ericsson-ca-deploy-bare-pod1'
    parameters:
        - node:
            name: SLAVE_NAME
            description: 'Slave name on Jenkins'
            allowed-slaves:
                - ericsson-ca-deploy-bare-pod1
            default-slaves:
                - ericsson-ca-deploy-bare-pod1
        - string:
            name: INSTALLER_VERSION
            default: latest
            description: 'Version of the installer to deploy'
        - string:
            name: GIT_BASE
            default: https://gerrit.opnfv.org/gerrit/$PROJECT
            description: 'Git URL to use on the jumphost'
        - string:
            name: YARDSTICK_TEST_SUITE
            default: /path/to/yardstick/ericsson-pod1/{loop}/suite
            description: 'Path to test suite'

- parameter:
    name: 'ericsson-ca-deploy-bare-pod2'
    parameters:
        - node:
            name: SLAVE_NAME
            description: 'Slave name on Jenkins'
            allowed-slaves:
                - ericsson-ca-deploy-bare-pod2
            default-slaves:
                - ericsson-ca-deploy-bare-pod2
        - string:
            name: INSTALLER_VERSION
            default: latest
            description: 'Version of the installer to deploy'
        - string:
            name: GIT_BASE
            default: https://gerrit.opnfv.org/gerrit/$PROJECT
            description: 'Git URL to use on the jumphost'

- parameter:
    name: 'intelpod2-jumphost'
    parameters:
        - node:
            name: SLAVE_NAME
            description: 'Slave name on Jenkins'
            allowed-slaves:
                - intelpod2-jumphost
            default-slaves:
                - intelpod2-jumphost
        - string:
            name: INSTALLER_VERSION
            default: stable
            description: 'Version of the installer to deploy'
        - string:
            name: GIT_BASE
            default: https://gerrit.opnfv.org/gerrit/$PROJECT
            description: 'Git URL to use on the jumphost'
        - string:
            name: SSH_KEY
            default: /root/.ssh/id_rsa
            description: 'SSH key to use for Apex'

- parameter:
    name: 'intel-us-deploy-bare-pod5'
    parameters:
        - node:
            name: SLAVE_NAME
            description: 'Slave name on Jenkins'
            allowed-slaves:
                - intel-us-deploy-bare-pod5
            default-slaves:
                - intel-us-deploy-bare-pod5
        - string:
            name: INSTALLER_VERSION
            default: stable
            description: 'Version of the installer to deploy'
        - string:
            name: GIT_BASE
            default: https://gerrit.opnfv.org/gerrit/$PROJECT
            description: 'Git URL to use on the jumphost'

- parameter:
    name: 'intel-us-deploy-bare-pod6'
    parameters:
        - node:
            name: SLAVE_NAME
            description: 'Slave name on Jenkins'
            allowed-slaves:
                - intel-us-deploy-bare-pod6
            default-slaves:
                - intel-us-deploy-bare-pod6
        - string:
            name: INSTALLER_VERSION
            default: latest
            description: 'Version of the installer to deploy'
        - string:
            name: GIT_BASE
            default: https://gerrit.opnfv.org/gerrit/$PROJECT
            description: 'Git URL to use on the jumphost'

- parameter:
    name: 'intel-us-deploy-bare-pod8'
    parameters:
        - node:
            name: SLAVE_NAME
            description: 'Slave name on Jenkins'
            allowed-slaves:
                - intel-us-deploy-bare-pod8
            default-slaves:
                - intel-us-deploy-bare-pod8
        - string:
            name: INSTALLER_VERSION
            default: latest
            description: 'Version of the installer to deploy'
        - string:
            name: GIT_BASE
            default: https://gerrit.opnfv.org/gerrit/$PROJECT
            description: 'Git URL to use on the jumphost'

- parameter:
    name: 'huawei-us-deploy-bare-1'
    parameters:
        - node:
            name: SLAVE_NAME
            description: 'Slave name on Jenkins'
            allowed-slaves:
                - huawei-us-deploy-bare-1
            default-slaves:
                - huawei-us-deploy-bare-1
        - string:
            name: INSTALLER_VERSION
            default: stable
            description: 'Version of the installer to deploy'
        - string:
            name: GIT_BASE
            default: https://gerrit.opnfv.org/gerrit/$PROJECT
            description: 'Git URL to use on the jumphost'

- parameter:
    name: 'intel-us-build-1'
    parameters:
        - node:
            name: SLAVE_NAME
            description: 'Slave name on Jenkins'
            allowed-slaves:
                - intel-us-build-1
            default-slaves:
                - intel-us-build-1
        - string:
            name: GIT_BASE
            default: https://gerrit.opnfv.org/gerrit/$PROJECT
            description: 'Git URL to use on the slave'

- parameter:
    name: 'intel-us-build-2'
    parameters:
        - node:
            name: SLAVE_NAME
            description: 'Slave name on Jenkins'
            allowed-slaves:
                - intel-us-build-2
            default-slaves:
                - intel-us-build-2
        - string:
            name: GIT_BASE
            default: https://gerrit.opnfv.org/gerrit/$PROJECT
            description: 'Git URL to use on the slave'

- parameter:
    name: 'ericsson-ca-build-1'
    parameters:
        - node:
            name: SLAVE_NAME
            description: 'Slave name on Jenkins'
            allowed-slaves:
                - ericsson-ca-build-1
            default-slaves:
                - ericsson-ca-build-1
        - string:
            name: GIT_BASE
            default: https://gerrit.opnfv.org/gerrit/$PROJECT
            description: 'Git URL to use on the slave'
