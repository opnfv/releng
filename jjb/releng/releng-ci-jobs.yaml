---
- project:
    name: releng-builder-jobs
    project: 'releng'
    project-name: 'releng'
    jjb-version: '2.5.0'
    build-timeout: 60
    project-pattern: 'releng'

    jobs:
      - '{project-name}-ci-jobs'
