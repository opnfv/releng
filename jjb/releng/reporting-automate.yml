- project:
    name: reporting-automate
    stream:
        - master:
            branch: '{stream}'

    phase:
        - 'docker-update'
        - 'docker-deploy':
            slave-label: 'testresults'

    jobs:
        - 'reporting-automate-{stream}'
        - 'reporting-automate-{phase}-{stream}'
        - 'reporting-verify-{stream}'

    project: 'releng'

- job-template:
    name: 'reporting-verify-{stream}'

    parameters:
        - project-parameter:
            project: '{project}'
            branch: '{branch}'
        - 'opnfv-build-ubuntu-defaults'

    scm:
        - git-scm-gerrit

    triggers:
        - gerrit:
            server-name: 'gerrit.opnfv.org'
            trigger-on:
                - patchset-created-event:
                    exclude-drafts: 'false'
                    exclude-trivial-rebase: 'false'
                    exclude-no-code-change: 'false'
                - draft-published-event
                - comment-added-contains-event:
                    comment-contains-value: 'recheck'
                - comment-added-contains-event:
                    comment-contains-value: 'reverify'
            projects:
              - project-compare-type: 'ANT'
                project-pattern: '{project}'
                branches:
                  - branch-compare-type: 'ANT'
                    branch-pattern: '**/{branch}'
                file-paths:
                  - compare-type: 'ANT'
                    pattern: 'utils/test/reporting/**'

    builders:
        - run-unit-tests

    publishers:
        - junit:
            results: nosetests.xml
        - cobertura:
            report-file: "coverage.xml"
            only-stable: "true"
            health-auto-update: "false"
            stability-auto-update: "false"
            zoom-coverage-chart: "true"
            targets:
                - files:
                    healthy: 10
                    unhealthy: 20
                    failing: 30
                - method:
                    healthy: 50
                    unhealthy: 40
                    failing: 30

- job-template:
    name: 'reporting-automate-{stream}'

    project-type: multijob

    properties:
        - throttle:
            enabled: true
            max-total: 1
            max-per-node: 1
            option: 'project'

    parameters:
        - project-parameter:
            project: '{project}'
            branch: '{branch}'
        - string:
            name: DOCKER_TAG
            default: "latest"
            description: "Tag name for testapi docker image"
        - 'opnfv-build-defaults'

    scm:
        - git-scm

    wrappers:
        - ssh-agent-wrapper
        - timeout:
            timeout: 360
            fail: true

    triggers:
        - gerrit:
            server-name: 'gerrit.opnfv.org'
            trigger-on:
                - change-merged-event
                - comment-added-contains-event:
                    comment-contains-value: 'remerge'
            projects:
              - project-compare-type: 'ANT'
                project-pattern: '{project}'
                branches:
                  - branch-compare-type: 'ANT'
                    branch-pattern: '**/{branch}'
                file-paths:
                  - compare-type: 'ANT'
                    pattern: 'utils/test/reporting/**'

    builders:
        - description-setter:
            description: "Built on $NODE_NAME"
        - multijob:
            name: docker-update
            condition: SUCCESSFUL
            projects:
                - name: 'reporting-automate-docker-update-{stream}'
                  current-parameters: true
                  kill-phase-on: FAILURE
                  abort-all-job: true
        - multijob:
            name: docker-deploy
            condition: SUCCESSFUL
            projects:
                - name: 'reporting-automate-docker-deploy-{stream}'
                  current-parameters: false
                  predefined-parameters: |
                    GIT_BASE=$GIT_BASE
                  node-label-name: SLAVE_LABEL
                  node-label: testresults
                  kill-phase-on: FAILURE
                  abort-all-job: true

    publishers:
        - 'email-publisher'

- job-template:
    name: 'reporting-automate-{phase}-{stream}'

    properties:
        - throttle:
            enabled: true
            max-per-node: 1
            option: 'project'

    parameters:
        - project-parameter:
            project: '{project}'
            branch: '{branch}'
        - string:
            name: DOCKER_TAG
            default: "latest"
            description: "Tag name for reporting docker image"

    wrappers:
        - ssh-agent-wrapper
        - timeout:
            timeout: 120
            fail: true

    scm:
        - git-scm

    builders:
        - description-setter:
            description: "Built on $NODE_NAME"
        - 'reporting-automate-{phase}-macro'

################################
# job builders
################################
- builder:
    name: 'run-unit-tests'
    builders:
        - shell: |
            bash ./utils/test/reporting/run-unit-tests.sh

- builder:
    name: 'reporting-automate-docker-update-macro'
    builders:
        - shell: |
            bash ./jjb/releng/docker-update.sh reporting

- builder:
    name: 'reporting-automate-docker-deploy-macro'
    builders:
        - shell: |
            bash ./jjb/releng/reporting-docker-deploy.sh

################################
# job publishers
################################

- publisher:
    name: 'email-publisher'
    publishers:
        - email:
            recipients: rohitsakala@gmail.com feng.xiaowei@zte.com.cn morgan.richomme@orange.com
            notify-every-unstable-build: false
            send-to-individuals: true
