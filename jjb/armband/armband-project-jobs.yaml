---
########################
# Job configuration for armband
########################
- project:
    name: armband-project-jobs

    project: 'armband'

    stream:
      - master:
          branch: '{stream}'
          gs-pathname: ''
          disabled: false
      - fraser:
          branch: 'stable/{stream}'
          gs-pathname: '/{stream}'
          disabled: false

    project-name: '{project}'
    project-pattern: '{project}'
    submodule-recursive: false
    rtd-build-url: https://readthedocs.org/api/v2/webhook/opnfv-armband/FIXME/
    rtd-token: FIXME

    jobs:
      - '{project-name}-rtd-jobs'
