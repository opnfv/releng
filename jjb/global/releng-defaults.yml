# jjb defaults

- defaults:
    name: global

    logrotate:
        daysToKeep: 60
        numToKeep: 200
        artifactDaysToKeep: 30
        artifactNumToKeep: 100

    ssh-credentials: *ssh-credential-id
    wrappers:
        - ssh-agent-wrapper

    project-type: freestyle

    node: master
