# jjb defaults

- defaults:
    name: global

    logrotate:
        daysToKeep: 60
        numToKeep: 200
        artifactDaysToKeep: 30
        artifactNumToKeep: 100

    ssh-credentials: 'd42411ac011ad6f3dd2e1fa34eaa5d87f910eb2e' &ssh-credentials
    wrappers:
        - ssh-agent-credentials:
            users:
                - '{ssh-credentials}'

    project-type: freestyle

    node: master

