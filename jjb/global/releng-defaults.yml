# jjb defaults

- defaults:
    name: global

    logrotate:
        daysToKeep: 60
        numToKeep: 200
        artifactDaysToKeep: 30
        artifactNumToKeep: 100

    wrappers:
        - ssh-agent-wrapper

    project-type: freestyle

    node: master
