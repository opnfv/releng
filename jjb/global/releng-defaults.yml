# jjb defaults

- defaults:
    name: global

    wrappers:
        - ssh-agent-wrapper

    project-type: freestyle

    node: master

    properties:
        - logrotate-default

    publishers:
        - email-admins
